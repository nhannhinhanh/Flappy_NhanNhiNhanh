<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Màu nền nếu cần -->
    <corners android:radius="40dp"/> <!-- Bán kính góc tròn (nửa chiều rộng/hơn) -->
</shape>
